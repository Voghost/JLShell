# JLShell v0.1.66

> 优化 Program 插件路由连接失败时的异常处理，界面现在只展示可读的业务错误信息。

## 修复

- SSH 正式连接和“测试连接”现在会完整解包异步异常，不再把 `CompletionException`、`ExecutionException` 或宿主内部异常类名展示给用户。
- 插件路由失败时仍会在应用日志中记录完整异常堆栈，便于排查，同时避免技术细节污染错误弹窗。
- 无可用错误信息时统一展示“未知错误”，避免空白提示。

## 兼容性

- 此版本不修改 Plugin API 或 Program API，现有插件无需迁移。
