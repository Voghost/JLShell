# JLShell v0.1.65

> 修复 Link 项目的测试连接绕过 Program 插件路由的问题，测试结果现在与正式连接保持一致。

## 修复

- 连接编辑窗口的“测试连接”现在会携带当前连接和项目信息，正确调用 Program 插件提供的 Link 路由。
- 测试连接成功或失败后都会释放临时 Link 隧道，避免残留 Connector 进程和本地监听端口。
- 测试使用的临时凭据现在始终在完成后清理。

## 兼容性

- 此版本不修改 Plugin API 或 Program API，现有 JLShell Link 插件无需迁移配置。
